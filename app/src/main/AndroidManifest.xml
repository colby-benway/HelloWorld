<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.colby.helloworld">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Super Ultra Mega Events"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".switchpage1"
            android:label="@string/title_activity_switchpage1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".switchpage2" />
        <activity android:name=".messagingPage" />
        <activity android:name=".LoginSelectionActivity" />
        <activity android:name=".LoginTypeSelectionActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupGroupSelectionActivity" />
        <activity android:name=".SignupInterestSelectionActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".MessagesScreen" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".ViewEventPage" />

        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCXbYbfRIf95sjgJZMcPektHleUMbn8jqQ" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>